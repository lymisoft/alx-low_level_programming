#include <stdio.h>

/**
 * div - a function that divides two integers and returns the result
 * @l: the first integer to div
 * @s: the second integer to div
 * Return: div of l and s
 */

int div(int l, int s)
{
        int t;

        t = l / s;

        return (t);
}
