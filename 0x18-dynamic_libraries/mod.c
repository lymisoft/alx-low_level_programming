#include <stdlib.h>

/**
 * mod - a function that mod two integers and returns the result
 * @l: the first integer to mod
 * @s: the second integer to mod
 * Return: mod of l and s
 */

int mod(int l, int s)
{
        int t;

        t = l % s;

        return (t);
}
