#include <stdlib.h>

/**
 * add - a function that adds two integers and returns the result
 * @l: the first integer to add
 * @s: the second integer to add
 * Return: sum of l and s
 */

int add(int l, int s)
{
        int t;

        t = l + s;

        return (t);
}
