#include <stdlib.h>

/**
 * sub - a function that subtracts two integers and returns the result
 * @l: the first integer to sub
 * @s: the second integer to sub
 * Return: sub of l and s
 */

int sub(int l, int s)
{
        int t;

        t = l - s;

        return (t);
}
