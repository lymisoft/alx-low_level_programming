#include <stdlib.h>

/**
 * mul - a function that multiplies two integers and returns the result
 * @l: the first integer to mul
 * @s: the second integer to mul
 * Return: mul of l and s
 */

int mul(int l, int s)
{
        int t;

        t = l * s;

        return (t);
}
