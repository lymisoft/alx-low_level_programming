#include "main.h"

/**
 * _abs - a function that computes the absolute value of an integer
 * @l: the integer to be checked
 * Return: the absolute value of the integer
 */

int _abs(int l)
{
	if (l >= 0)
	{
		return (l);
	}
	return (-l);
}
